{
  "name": "develop-with-derek",
  "version": "0.9.0",
  "description": "personal-portfolio",
  "main": "app.js",
  "scripts": {
    "dev": "concurrently \"nodemon app.js --ignore public\" \"sass -w sass/index.scss:public/style.css\" \"browser-sync start --proxy localhost:8080 --files *\" ",
    "test": "test",
    "start": "node app.js"
  },
  "author": "Derek M. Robertson",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "multer": "^1.4.2",
    "pug": "^3.0.2",
    "serve-favicon": "^2.5.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.7",
    "rfs": "^9.0.4",
    "sass": "^1.35.1"
  }
}
